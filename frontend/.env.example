# Render Backend URL for decoupled hosting
# Leave empty for local development (uses Vite reverse-proxy)
# Set to your live Render backend URL when deploying frontend on Vercel:
VITE_API_URL=https://your-backend-app.onrender.com
